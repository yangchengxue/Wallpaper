package com.example.businessbase.utils

import android.net.Uri
import java.io.File

/*
fun localPath(localPathUri: String) {
    val downloadFile = File(localPathUri)
    val parentFile = downloadFile.parentFile
    if (!parentFile.exists()) {
        parentFile.mkdirs()
    }
    this.localPathUri = Uri.fromFile(downloadFile)
    return this
}*/
